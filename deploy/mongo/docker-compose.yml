services:
  mongodb:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: pdf_agent
    volumes:
      - pdf_agent_mongo_data:/data/db

volumes:
  pdf_agent_mongo_data:
