# Langfuse 自托管（pdf-agent）

```bash
# 从仓库根目录
bash scripts/start_langfuse.sh
```

- UI：http://localhost:3000  
- 配置：复制 `.env.example` → `.env`，首次启动后把 API Keys 写入仓库根目录 `.env`  

完整说明：[docs/integrations/LANGFUSE.md](../../docs/integrations/LANGFUSE.md)
