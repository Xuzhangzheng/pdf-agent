# MinerU / magic-pdf 额外约束（由 scripts/fix_mineru_env.sh 安装）
# magic-pdf 1.3.12 要求 transformers>=4.49,<5，但 4.54+ 与 UniMERNet 不兼容
transformers==4.52.4
tokenizers==0.21.1
numpy==1.26.4
opencv-python-headless==4.10.0.84
pycocotools>=2.0.6
